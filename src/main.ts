export { wrap, Pools } from './pool';

export { _v } from './vect-functions';
export { _s } from './segment-functions';

export { Coord } from './coord';

// the Vect constructor is used under the hood only
export { Vect, ABSCISSA, ORDINATE, ZERO } from './vect';

export { SQRT_OF_2, TAU, degrees, radians } from './utils';

// const defaultInstance = new Garfunkel();
